defaults: &defaults
  token: <%= ENV["TOKEN"] %>
  token_url: "orcid.org/oauth/token"
  api_url: "pub.orcid.org"
  api_version: "3.0"
test:
  <<: *defaults
development:
  <<: *defaults
production:
  <<: *defaults
  api_url: "api.orcid.org"
staging:
  <<: *defaults
